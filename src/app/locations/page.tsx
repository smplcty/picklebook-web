"use client";
import Link from "next/link"; import { useQuery } from "@tanstack/react-query"; import { api } from "@/lib/api";
type Location={id:string;name:string;address:string;courts:{id:string}[]};
export default function LocationsPage(){const {data,isLoading}=useQuery({queryKey:["locations"],queryFn:()=>api<Location[]>("/locations")});return <main className="mx-auto max-w-6xl px-5 py-10"><p className="mono text-xs font-bold text-[#65a830]">01 / PICK A VENUE</p><h1 className="mt-2 text-4xl font-extrabold">Where are we playing?</h1><div className="mt-8 grid gap-5 md:grid-cols-2">{isLoading&&<p>Loading locations…</p>}{data?.map((x,i)=><article key={x.id} className="group overflow-hidden rounded-[1.6rem] border border-[#112219]/10 bg-white shadow-sm transition hover:-translate-y-1 hover:shadow-xl"><div className={`h-28 p-5 ${i%2?"bg-[#b8f44c]":"bg-[#112219]"}`}><span className={`mono text-xs font-bold ${i%2?"text-[#112219]":"text-[#b8f44c]"}`}>VENUE 0{i+1}</span><strong className={`float-right text-4xl ${i%2?"text-[#112219]":"text-white"}`}>{x.courts.length}</strong></div><div className="p-6"><h2 className="text-2xl font-extrabold">{x.name}</h2><p className="mt-2 text-sm text-[#607166]">{x.address}</p><Link className="mt-5 block rounded-xl bg-[#112219] px-4 py-3 text-center text-sm font-bold text-white group-hover:bg-[#65a830]" href={`/book/${x.id}`}>See available courts →</Link></div></article>)}</div></main>}
