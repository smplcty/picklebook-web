"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
import { api } from "@/lib/api";
export default function RegisterPage() { const router = useRouter(); const [error, setError] = useState(""); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const f = new FormData(event.currentTarget); try { const result = await api<{ accessToken: string }>("/auth/register", { method: "POST", body: JSON.stringify({ firstName: f.get("firstName"), lastName: f.get("lastName"), email: f.get("email"), password: f.get("password") }) }); localStorage.setItem("accessToken", result.accessToken); router.push("/locations"); } catch (reason) { setError(reason instanceof Error ? reason.message : "Unable to create account."); } } return <main className="mx-auto max-w-md p-6"><h1 className="text-3xl font-bold">Create your account</h1><form className="mt-6 grid gap-3" onSubmit={submit}><input className="rounded-lg border p-3" name="firstName" placeholder="First name" required /><input className="rounded-lg border p-3" name="lastName" placeholder="Last name" required /><input className="rounded-lg border p-3" name="email" type="email" placeholder="Email" required /><input className="rounded-lg border p-3" name="password" type="password" minLength={8} placeholder="Password (8+ characters)" required /><button className="rounded-lg bg-slate-950 p-3 font-semibold text-white">Create account</button>{error && <p className="text-sm text-red-700">{error}</p>}</form></main>; }
